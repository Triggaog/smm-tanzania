import{PageTop}from"@/components/site";import{db}from"@/lib/prisma";import Link from"next/link";export const dynamic='force-dynamic';export default async function Page(){const[posts,page]=await Promise.all([db.contentItem.findMany({where:{type:'post',status:'PUBLISHED'},orderBy:{displayOrder:'asc'},select:{id:true,slug:true,title:true,summary:true,data:true}}),db.contentItem.findUnique({where:{type_slug:{type:'page',slug:'blog'}},select:{data:true}})]);const d=(page?.data||{})as any;return <><PageTop label={d.label} heading={d.heading} copy={d.description}/><section><div className="wrap blog-grid">{posts.map((p,i)=><Link className="post" href={`/blog/${p.slug}`} key={p.id}><div className={`post-image p${i%4}`}><span>{(p.data as any)?.category}</span></div><div><h3>{p.title}</h3><p>{p.summary}</p></div></Link>)}</div></section></>}
