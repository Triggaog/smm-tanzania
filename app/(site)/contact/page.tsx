import{PageTop}from"@/components/site";import{db}from"@/lib/prisma";export const dynamic='force-dynamic';export default async function Page(){const[p,s]=await Promise.all([db.contentItem.findUnique({where:{type_slug:{type:'page',slug:'contact'}},select:{data:true}}),db.contentItem.findFirst({where:{type:'setting',status:'PUBLISHED'},select:{data:true}})]);const d=(p?.data||{})as any,c=(s?.data||{})as any;return <><PageTop label={d.label} heading={d.heading} copy={d.description}/><section><div className="wrap form-layout"><div><h2>{c.location}</h2><p>{c.email}<br/>{c.phone}</p></div><form action="/api/submit" method="post"><input type="hidden" name="type" value="contact"/><label>Full name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone"/></label><label>Message<textarea name="message" required/></label><button className="button">Send Message</button></form></div></section></>}
