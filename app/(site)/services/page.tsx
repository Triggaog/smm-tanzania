import{PageTop}from"@/components/site";import{ServiceIcon}from"@/components/feature-icon";import{db}from"@/lib/prisma";import Link from"next/link";export const dynamic='force-dynamic';export default async function Page(){const[services,page]=await Promise.all([db.service.findMany({where:{status:'PUBLISHED',active:true},orderBy:{displayOrder:'asc'},select:{id:true,slug:true,name:true,heroDescription:true}}),db.contentItem.findUnique({where:{type_slug:{type:'page',slug:'services'}},select:{data:true}})]);const d=(page?.data||{})as any;return <><PageTop label={d.label} heading={d.heading} copy={d.description}/><section><div className="wrap service-grid">{services.map((s,i)=><Link className="service" href={`/services/${s.slug}`} key={s.id}><span className="service-no">0{i+1}</span><div className={`service-visual v${i%4}`}><span className="visual-mark"><ServiceIcon slug={s.slug}/></span></div><h3>{s.name}</h3><p>{s.heroDescription}</p></Link>)}</div></section></>}
