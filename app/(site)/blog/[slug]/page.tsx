import {PageTop} from "@/components/site";import {getPublishedPost} from "@/lib/public-queries";import {notFound} from "next/navigation";import type{Metadata}from"next";export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=await getPublishedPost(slug);return p?{title:p.seoTitle||p.title,description:p.seoDescription||p.summary||''}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=await getPublishedPost(slug);if(!p)notFound();return <><PageTop label={(p.data as any)?.category||'Insights'} heading={p.title} copy={p.summary||''}/><article className="wrap article"><p>{(p.data as any)?.content}</p></article></>}
