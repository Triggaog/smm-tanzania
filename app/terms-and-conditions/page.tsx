import{PageTop}from"@/components/site";import{db}from"@/lib/prisma";export const dynamic='force-dynamic';export default async function Page(){const p=await db.contentItem.findUnique({where:{type_slug:{type:'page',slug:'terms-and-conditions'}}});const d=(p?.data||{})as any;return <><PageTop label={d.label} heading={d.heading} copy={d.description}/><section><div className="wrap article">{d.sections?.map((x:any,i:number)=><div key={i}><h2>{x.heading}</h2><p>{x.content}</p></div>)}</div></section></>}
