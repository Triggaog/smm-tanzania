import {NextRequest,NextResponse} from "next/server";import {jwtVerify} from "jose";export async function proxy(req:NextRequest){if(req.nextUrl.pathname==="/admin/login"||req.nextUrl.pathname==="/api/admin/login")return NextResponse.next();const token=req.cookies.get("smm_admin")?.value;if(!token)return req.nextUrl.pathname.startsWith('/api/')?NextResponse.json({error:'Unauthorized'},{status:401}):NextResponse.redirect(new URL('/admin/login',req.url));try{await jwtVerify(token,new TextEncoder().encode(process.env.SESSION_SECRET||"development-secret-change-this-value"));return NextResponse.next()}catch{return NextResponse.redirect(new URL('/admin/login',req.url))}}export const config={matcher:["/admin/:path*","/api/admin/:path*"]}
