import {SignJWT,jwtVerify} from "jose";import {cookies} from "next/headers";export const sessionCookie="smm_admin";const secret=()=>new TextEncoder().encode(process.env.SESSION_SECRET||"development-secret-change-this-value");export async function signSession(id:string,email:string){return new SignJWT({sub:id,email}).setProtectedHeader({alg:"HS256"}).setIssuedAt().setExpirationTime("8h").sign(secret())}export async function verifySession(token?:string){if(!token)return null;try{return(await jwtVerify(token,secret())).payload}catch{return null}}export async function requireAdmin(){const jar=await cookies();return verifySession(jar.get(sessionCookie)?.value)}
