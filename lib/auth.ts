import {SignJWT} from "jose";export const sessionCookie="smm_admin";const secret=()=>new TextEncoder().encode(process.env.SESSION_SECRET||"development-secret-change-this-value");export async function signSession(id:string,email:string){return new SignJWT({sub:id,email}).setProtectedHeader({alg:"HS256"}).setIssuedAt().setExpirationTime("8h").sign(secret())}
