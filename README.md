# SMM Tanzania

Minimal Next.js agency website inspired by the clear page flow and spacious presentation of Optiza, with original SMM Tanzania branding and content.

```bash
npm install
npm run dev
```

All public pages are handled by one optional catch-all page file. Contact and booking submissions are handled by one API route and logged safely in local development.
